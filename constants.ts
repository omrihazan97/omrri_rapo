import { ExpenseItem, CategoryType } from './types';

// Data is now initialized as empty so the user can enter their own.
export const INITIAL_DATA: ExpenseItem[] = [];

export const CHART_COLORS = ['#ec4899', '#fbbf24', '#3b82f6', '#10b981', '#8b5cf6', '#6366f1'];